/// @description Insert description here
// You can write your code in this editor

if (room == room4) {
	path_start(pth_ast,.1,speed = 0,true);
	path_position = pth_pos;
}

if (room == room5) {
	path_start(pth_ast,.15,speed = 0,true);
	path_position = pth_pos + .1;
}